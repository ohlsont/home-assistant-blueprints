# Add this to /config/configuration.yaml
#
# If you already have a top-level `homeassistant:` block,
# only add the `packages` line under it.
# Do not create duplicate top-level `homeassistant:` keys.

homeassistant:
  packages: !include_dir_named packages
